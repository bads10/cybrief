import 'package:flutter/material.dart';

class AppColors {
  // Fonds
  static const background      = Color(0xFF0D1117);
  static const surface         = Color(0xFF161B22);
  static const surfaceElevated = Color(0xFF21262D);
  static const border          = Color(0xFF30363D);

  // Accent cyan — signature Cybrief
  static const cyan            = Color(0xFF00D4FF);
  static const cyanDark        = Color(0xFF0099BB);
  static const cyanSurface     = Color(0xFF0D2733);

  // Sévérité
  static const critical        = Color(0xFFFF3B30);
  static const criticalBg      = Color(0xFF2D1117);
  static const high            = Color(0xFFFF6B35);
  static const highBg          = Color(0xFF2D1A0F);
  static const medium          = Color(0xFFFFCC00);
  static const mediumBg        = Color(0xFF2D2500);
  static const low             = Color(0xFF30D158);
  static const lowBg           = Color(0xFF0D2D1A);

  // Texte
  static const textPrimary     = Color(0xFFE6EDF3);
  static const textSecondary   = Color(0xFF8B949E);
  static const textMuted       = Color(0xFF484F58);

  // CTA
  static const ctaOrange       = Color(0xFFFF9500);

  // Premium
  static const premiumGold     = Color(0xFFFFD60A);
}
